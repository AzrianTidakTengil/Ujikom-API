'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up (queryInterface, Sequelize) {
    /**
     * Add altering commands here.
     *
     * Example:
     * await queryInterface.createTable('users', { id: Sequelize.INTEGER });
     */

    await queryInterface.addColumn('Transactions', 'payment_type', { type: Sequelize.STRING})
    await queryInterface.addColumn('Transactions', 'subtype', { type: Sequelize.STRING})
    await queryInterface.removeColumn('Transactions', 'trolley_id')

  },

  async down (queryInterface, Sequelize) {
    /**
     * Add reverting commands here.
     *
     * Example:
     * await queryInterface.dropTable('users');
     */
  }
};
